uses 'ClearIt.feh';

namespace ExternalQueueInterface {
	void queue;
	
	function name() {
		return .queue.name();
	}
	
	function add( object errand ) {
		.queue.add(errand);
	}
	function remove( object errand ) {
		.queue.remove(errand);
	}
}

ExternalQueueInterface.queue = ClearItQueue;
